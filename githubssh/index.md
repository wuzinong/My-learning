# 生成 ssh

# Go to github -> SSH and GPG keys

# 添加并且勾选需要认证的 orgnization

## 1.新项目 clone 选 ssh 即可

## 老项目找到.git -> config 把 remote origin 下面的 url 替换成 基于 ssh 的 it@github.com:orgnization/xxx.git

# 如果替换新的 ssh key 的话渠道 Users -> 用户名 -> .ssh 下面把 id_xxx 喝 id_xxx.pub 删除，并且替换成新的
